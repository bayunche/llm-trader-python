{
  "known": [
    "配置 API（src/llm_trader/api/routes/config_models.py）在模块顶层实例化 ModelGateway，并在 upsert 后调用 _refresh_gateway_from_db 刷新内存配置。",
    "ModelGateway 默认通过 create_session_factory 连接 PostgreSQL；在本地无数据库时连接会长时间等待。",
    "tests/api/test_config_models.py 依赖 monkeypatch.setattr(config_models, \"gateway\", stub_gateway) 注入桩实现。",
    "managed_cycle/_ensure_decision_services 会在需要时再创建 ModelGateway/Actor/Checker 服务，说明系统其他部分已经采用惰性模式。"
  ],
  "unknown": [
    "高优先级：如何在 config_models 中实现惰性 gateway 获取，既避免导入即连库，又保持 test_config_models 通过 monkeypatch 替换 gateway 的能力？",
    "中优先级：惰性 gateway 引入后，是否需要暴露 reset/close 钩子，供测试或配置更新后重建 httpx.Client 与统计数据？",
    "中优先级：文档与验证记录应如何描述本次修复——需要在 docs/重构计划.md 中将 Task 3 的阻塞点标记为已解决，还是单独新增一条备注？"
  ],
  "notes": "聚焦 config_models 模块的初始化副作用与测试策略；完成惰性化后应验证 API、managed_cycle 与决策服务之间的依赖是否仍能正确刷新。"
}
