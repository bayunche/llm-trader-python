{
  "known": [
    "run_full_automation 已在交易执行后写入 trading_runs parquet，记录 prompt/response 等字段。",
    "调度器任务 run_cycle 仍停留在日志输出，未复用上述写入逻辑，因此触发的自动交易无法在仪表盘历史页呈现。",
    "仪表盘主页面只有三个标签，新标签缺位导致用户无法快速定位自动交易大模型调用详情。",
    "dashboard.data 已提供 get_history/get_recent_llm_logs 等函数，可复用以构建独立视图。"
  ],
  "unknown": [
    "高优先级：在调度路径中调用哪段代码最合适以复用 trading_runs 写入逻辑，避免重复实现？",
    "高优先级：新增的自动交易调用日志 tab 需要展示哪些字段（如 prompt、response、风控结论、选股列表），是否需额外聚合？",
    "中优先级：缓存命中后新写入的 trading_runs 是否会被仪表盘立即读取，是否需要在界面层触发 invalidate_cache？",
    "低优先级：是否需要为新标签添加导出功能或按策略/会话过滤的默认选项？"
  ],
  "notes": "接下来重点验证 run_ai_trading_cycle 返回结构是否始终包含 config 与 LLM 字段，并在设计仪表盘新 tab 时考虑缓存刷新提示。"
}
